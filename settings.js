const Secrets = require('./secrets');

const settings = {
    LOGIN_DELAY : 300000,
    LOGFILE_NAME : 'log.txt',
    LEAGUE_ID : '492760',
    TEAM_ID : '10',
    SECRETS : Secrets
}

module.exports = settings;